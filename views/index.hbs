{{!-- <h1>{{ title }}</h1>
<p>Welome to {{ title }}</p>
{{# if condition }}
    condition is met
{{ else }}
    condition not met
{{/if }}

{{# each anyArray as |val key| }}
    {{ key }} : {{ val }}
{{/each}} --}}

    <!-- Page Content -->
    <div>
        <br>
        <br>
    </div>
    <div class="container">
      <div class="row">
        <div class="col-lg-12 text-center">
          <h1 class="mt-5">Shawn Chaudhry's Guestbook</h1>
          <p class="lead">Please sign my Guest Book by Clicking below:</p>
            <button type="submit" id="submit" class="btn btn-primary" onclick="signGuestBook()">Submit</button>
        </div>
      </div>
    </div>
    <div>
        <br>
        <br>
    </div>
    <script>
        function signGuestBook(){
            window.location.href = "http://localhost:3002/signup"
        }
    </script>

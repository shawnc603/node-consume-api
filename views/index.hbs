<h1>{{ title }}</h1>
<p>Welome to {{ title }}</p>
{{# if condition }}
    condition is met
{{ else }}
    condition not met
{{/if }}

{{# each anyArray as |val key| }}
    {{ key }} : {{ val }}
{{/each}}
